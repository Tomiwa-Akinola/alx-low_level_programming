#include "main.h"

/**
 * _atoi - converts a string to an integer
 * @s: parameter
 * Return: 0 if there are no numbers
 */

int _atoi(char *s)
{
	int i = 0;
	int j = 1;
	unsigned int k = 0;
	int l = 0;

	while (s[c])
	{
		if (s[c] == 45)
		{
			j *= -1;
		}
	}

}
